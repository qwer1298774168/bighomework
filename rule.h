#ifndef RULE_H
#define RULE_H

#include <QMainWindow>

class Rule : public QMainWindow
{
    Q_OBJECT
public:
    explicit Rule(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // RULE_H